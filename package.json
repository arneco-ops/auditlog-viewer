{
  "name": "audit-log-viewer",
  "version": "1.0.0",
  "description": "SAP CAP Audit Log Viewer Application",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^7",
    "@sap/xssec": "^3",
    "express": "^4",
    "axios": "^1.6.0",
    "passport": "^0.7"
  },
  "devDependencies": {
    "@sap/cds-dk": "^7",
    "@sap/ux-specification": "^1.120.0",
    "@ui5/cli": "^3.0.0",
    "sqlite3": "^5"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "watch-audit-viewer": "cds watch --open audit-viewer/webapp/index.html",
    "build": "cds build --production",
    "build:mta": "mbt build",
    "deploy": "cf deploy mta_archives/auditlog-viewer_1.0.0.mtar"
  },
  "engines": {
    "node": "^18 || ^20"
  },
  "cds": {
    "requires": {
      "db": false,
      "auth": {
        "[production]": {
          "kind": "xsuaa"
        }
      }
    }
  }
}
